# Prototypes Directory

一次性原型代码。与 `src/` 完全隔离，不影响主线代码。

## 使用方式

- 运行 `/prototype [game-mechanic]` 创建新原型
- 原型代码不需要遵循 `src/` 的编码标准
- 原型完成后，将需要的代码迁移到 `src/`，然后删除原型
- 每个原型放在独立子目录中

## 原型规范

- 快速验证核心玩法
- 可以用简化版美术/音频资源
- 不要求完整的错误处理
- 开发周期：1-3 天
